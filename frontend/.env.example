# SAMPLE CONFIG FILE for SpotiRecap frontend
# Copy this file to `.env` in the root frontend directory and fill in your own credentials

# NOTE: You can keep everything as-is except for the VITE_CLIENT ID
# - replace it with your own from the Spotify Developer Dashboard.

# Your Spotify application's client ID (from https://developer.spotify.com/dashboard)
VITE_CLIENT_ID=your_spotify_client_id

# The redirect URI registered in your Spotify Developer Dashboard
VITE_REDIRECT_URI=http://localhost:3000/callback

# Backend server URL (Spring Boot app)
# Default is local development URL
VITE_BACKEND_URL=http://localhost:8080
